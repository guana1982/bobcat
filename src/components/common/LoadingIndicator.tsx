import * as React from "react";
import * as styles from "./LoadingIndicator.scss";

export default () => {
  return (
    <div className={styles.loading}>
      <label>Loading...</label>
    </div>
  );
};
